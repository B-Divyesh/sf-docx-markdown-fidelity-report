[
  {
    "id": "demo-conversion",
    "claim": "The demo converts a bundled DOCX and writes Markdown plus a fidelity report.",
    "where": "landing first screen, demo, README",
    "test": "npm test -- --grep @claim:demo-conversion",
    "sandbox": "fresh temporary directory; bundled sample; assert Markdown and report files plus review findings"
  },
  {
    "id": "local-processing",
    "claim": "Document conversion runs locally and sends no document data over the network.",
    "where": "landing facts, privacy, README",
    "test": "npm test -- --grep @claim:local-processing",
    "sandbox": "fresh browser context on /?demo=1; intercept requests and assert only same-origin site assets load"
  },
  {
    "id": "fidelity-report",
    "claim": "Fidelity reports list tables, comments, revisions, embedded objects, footnotes, styles, and image outcomes with source locations.",
    "where": "landing preview, demo, README",
    "test": "npm test -- --grep @claim:fidelity-report",
    "sandbox": "bundled complex DOCX fixture; assert every construct category and source location in JSON report"
  },
  {
    "id": "source-fidelity",
    "claim": "Literal Word text stays literal in Markdown; ordered, bullet, and nested lists are preserved or source-located for review.",
    "where": "landing report preview, README",
    "test": "npm test -- --grep @claim:source-fidelity",
    "sandbox": "fresh DOCX fixtures with Markdown-looking text, decimal and nested numbering.xml definitions, and an unknown list; assert literal output, preserved list syntax, and a source-located loss finding"
  },
  {
    "id": "batch-conversion",
    "claim": "One command converts every DOCX in a directory.",
    "where": "landing how it works, README",
    "test": "npm test -- --grep @claim:batch-conversion",
    "sandbox": "fresh directory with two DOCX fixtures whose names normalize to the same safe stem; assert two distinct Markdown files and reports"
  },
  {
    "id": "ci-policy",
    "claim": "The --fail-on option stops automated checks at selected risk levels without a network connection.",
    "where": "landing automated-check section, README",
    "test": "npm test -- --grep @claim:ci-policy",
    "sandbox": "bundled complex DOCX with blocked findings; assert --fail-on exits 3 while HTTP proxies are unreachable"
  },
  {
    "id": "safe-input",
    "claim": "The CLI limits ZIP and XML parts, rejects unsafe archive paths, and never extracts embedded objects or macros.",
    "where": "landing boundaries, README",
    "test": "npm test -- --grep @claim:safe-input",
    "sandbox": "malicious path, embedded-object/macro, 32 MiB plus one byte, and 10,001-entry DOCX fixtures; assert safe failure and no binary extraction"
  },
  {
    "id": "demo-isolation",
    "claim": "The sample demo uses separate demo storage; Reset demo and Start for real clear only demo state.",
    "where": "demo banner, privacy, README, .factory/demo.md",
    "test": "npm test -- --grep @claim:demo-isolation",
    "sandbox": "fresh browser demo context; seed demo: and real: keys, activate Reset demo and Start for real, and assert only demo: keys are removed"
  },
  {
    "id": "copy-commands",
    "claim": "Copy commands copies the install commands to the clipboard.",
    "where": "landing install button",
    "test": "npm test -- --grep @claim:copy-commands",
    "sandbox": "fresh browser context; activate Copy commands with the keyboard and read the browser clipboard"
  },
  {
    "id": "replay-recording",
    "claim": "Replay recording restarts the sample conversion recording from the first command.",
    "where": "demo terminal control",
    "test": "npm test -- --grep @claim:replay-recording",
    "sandbox": "fresh browser demo context; let the recording finish, activate Replay recording with the keyboard, assert it begins again, then assert it reaches the sandbox path"
  },
  {
    "id": "sample-markdown",
    "claim": "The sample Markdown includes a heading, link, table, image reference, and footnote.",
    "where": "demo result",
    "test": "npm test -- --grep @claim:sample-markdown",
    "sandbox": "CLI demo in a fresh temporary directory; assert the bundled sample Markdown contains every named structure"
  },
  {
    "id": "review-checklist",
    "claim": "Each conversion writes a separate human review checklist.",
    "where": "landing steps, README",
    "test": "npm test -- --grep @claim:review-checklist",
    "sandbox": "CLI demo from a fresh temporary directory; assert the generated .fidelity.md file contains a review checklist and finding checkboxes"
  },
  {
    "id": "single-binary",
    "claim": "The release build produces one Rust CLI binary.",
    "where": "landing install section, README",
    "test": "npm test -- --grep @claim:single-binary",
    "sandbox": "locked release build; assert target/release/docx-fidelity exists and reports its version"
  },
  {
    "id": "rust-toolchain",
    "claim": "Stable Rust 1.88 or newer builds the locked package.",
    "where": "README install and verification instructions, Cargo.toml",
    "test": "npm test -- --grep @claim:rust-toolchain",
    "sandbox": "run cargo +1.88.0 test --locked --lib and assert the locked library test suite passes"
  },
  {
    "id": "scope-boundaries",
    "claim": "The CLI leaves source DOCX files unchanged and offers no OCR, document editing, or PDF conversion command.",
    "where": "landing boundaries, README, terms",
    "test": "npm test -- --grep @claim:scope-boundaries",
    "sandbox": "convert a copied bundled DOCX, byte-compare the source before and after, and assert CLI help has no OCR, edit, or PDF command"
  },
  {
    "id": "mit-license",
    "claim": "The source code is MIT licensed.",
    "where": "README, terms",
    "test": "npm test -- --grep @claim:mit-license",
    "sandbox": "fresh checkout; assert the shipped LICENSE contains the MIT grant and Cargo metadata declares MIT"
  }
]
